<template name="loading">
  <view class="loading" wx:if="{{show}}">
    <!-- <view class="loading-mask"></view> -->
    <view class="loading-body" wx:if="{{showIcon}}">
      <view class="loading-icon"></view>
    </view>
  </view>
</template>