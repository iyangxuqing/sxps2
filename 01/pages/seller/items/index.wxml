<import src="/template/itemsGrid/index.wxml" />

<view class="container" wx:if="{{ready}}">
  <view class="seller-items">
    <template is="itemsGrid" data="{{...itemsGrid, youImageMode}}" />
  </view>
</view>